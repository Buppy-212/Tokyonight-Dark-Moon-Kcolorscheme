(defwidget bluetooth []
  (box :orientation "h" :class "vbluetooth"
    (button :onclick "uwsm app -- kitty --title=minitty -e bluetui &" :class "bluetooth button" :tooltip "Bluetooth"
      (label :text "󰂯" :class "bluetooth icon")
    )
  )
)

(defwidget vbluetooth []
  (box :orientation "v" :class "vbluetooth"
    (button :onclick "uwsm app -- kitty --title=leftty -e bluetui &" :class "vbluetooth button" :tooltip "Bluetooth"
      (label :text "󰂯" :class "vbluetooth icon")
    )
  )
)
